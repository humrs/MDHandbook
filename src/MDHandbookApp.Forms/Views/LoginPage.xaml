<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MDHandbookApp.Forms.Views.LoginPage"
             Title="Login">
  <StackLayout Margin="20">
    <Label FontSize="Medium" Text ="Login with one of the following providers." />
    <Button Text="Google"    Command="{Binding LoginGoogle}" />
    <Button Text="Facebook"  Command="{Binding LoginFacebook}"/>
    <Button Text="Microsoft" Command="{Binding LoginMicrosoft}"/>
    <Button Text="Twitter"   Command="{Binding LoginTwitter}" />
    <Button Text="Return to MainPage" Command="{Binding NavigateToMainPage}" />
  </StackLayout>
</ContentPage>